type Transaction = {
  id: number;
  transactionNumber: string;
  totalAmount: number;
  active: boolean;
  created_date: string;
  updated_date: string;
  updated_user: string;
  created_user: string;
};

export default Transaction;
